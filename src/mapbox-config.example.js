/**
 * Local setup: copy this file to mapbox-config.js and set your public access token.
 *   Windows: copy src\mapbox-config.example.js src\mapbox-config.js
 * GitHub Pages: add repository secret MAPBOX_ACCESS_TOKEN; the deploy workflow writes mapbox-config.js.
 *
 * Restrict the token by URL in Mapbox Account → Tokens (localhost + your github.io domain).
 */
export const MAPBOX_ACCESS_TOKEN = "";
